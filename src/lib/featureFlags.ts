export const ARENA_ENABLED = true;
export const ARENA_SAFE_FALLBACK = true; // DEV-ONLY. Set false or remove later.

// Arena v2 (new isolated schema/rpcs)
export const ARENA_V2 = true; // flip to false to instantly fall back to old UI