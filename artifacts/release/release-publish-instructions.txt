gh release create v2.0.0 --draft --title "Arena V2 (v2.0.0)" --notes-file .github/release-draft.md

# Optionally attach build artifacts after CI completes:
gh release upload v2.0.0 dist/** --clobber

# Or attach from CI artifacts:
# Download web-build artifact from Actions and upload
gh release upload v2.0.0 web-build.zip --clobber